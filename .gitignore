node_modules/
dist/
.wrangler/
.dev.vars
*.log
.astro/
.DS_Store

# Model artifacts are copied into public/models from model/artifacts by the
# build script; the large ONNX files are tracked there, not duplicated here.

# Vast.ai API key
VASTAI_API_KEY.txt

# OpenRouter API key (local secret for the cloud digitize path)
OPENROUTER_API_KEY.txt