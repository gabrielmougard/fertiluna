.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/

# Pre-rendered synthetic chart datasets are large and regenerable : never commit
# them. Everything else under artifacts/ (trained .ckpt / .onnx / manifests) IS
# version-controlled so the shipped models are reproducible.
data/
*.npz
